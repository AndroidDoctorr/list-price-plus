# Firebase Web API key (public — safe to embed in client apps).
# Create a Web app in Firebase Console → Project settings → Your apps.
# Use the same key for both extension and web builds.

VITE_FIREBASE_API_KEY=
WXT_FIREBASE_API_KEY=
