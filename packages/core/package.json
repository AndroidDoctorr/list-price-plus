{
  "name": "@list-price-plus/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "compile": "tsc --noEmit",
    "test": "vitest run",
    "build": "echo \"@list-price-plus/core: types consumed via source\""
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
